# dns.py
Security tool to scan for subdomains. It will bruteforce common sub-domains, check Certificate Transparency logs, check the TLS alternative names of each server and lookup reverse domains including preceding and subsequent IP addresses. 

usage
python dns.py example.com
